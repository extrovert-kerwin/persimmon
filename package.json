{
  "name": "extrovert",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://extrovert-kerwin.github.io/persimmon",
  "dependencies": {
    "antd": "^5.22.3",
    "gh-pages": "^2.0.1",
    "react": "^16.8.3",
    "react-dom": "^16.8.3",
    "react-scripts": "2.1.5",
    "sticky-kit": "^1.1.3",
    "travis": "^0.1.1",
    "yarn": "^1.13.0"
  },
  "scripts": {
    "start": "set NODE_OPTIONS=--openssl-legacy-provider && react-scripts start",
    "build": " set NODE_OPTIONS=--openssl-legacy-provider && react-scripts build",
    "predeploy": "set NODE_OPTIONS=--openssl-legacy-provider && yarn run build",
    "deploy": "gh-pages -d build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
